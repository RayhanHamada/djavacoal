import {
    deletePhoto,
    generateUploadUrl,
    getContactSettings,
    getPhotoList,
    getReels,
    getYouTubeUrl,
    saveContactSettings,
    savePhotoList,
    saveReels,
    saveYouTubeUrl,
} from "./functions";

export const router = {
    generateUploadUrl,
    savePhotoList,
    getPhotoList,
    saveYouTubeUrl,
    getYouTubeUrl,
    saveReels,
    getReels,
    deletePhoto,
    saveContactSettings,
    getContactSettings,
};
